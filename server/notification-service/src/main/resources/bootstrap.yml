spring:
  application:
    name: notification-service

  cloud:
    config:
      uri: http://localhost:8888
    refresh:
      enabled: false

  data:
    mongodb:
      database: notification
      host: localhost
      port: 27017
